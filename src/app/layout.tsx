import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    template: '%s | KhGlobal',
    default: 'KhGlobal - Premier Multi-Vendor Marketplace',
  },
  description: "Discover top-quality products at KhGlobal. The premier multi-vendor marketplace connecting trusted sellers with buyers worldwide.",
  keywords: ["KhGlobal", "multi-vendor", "marketplace", "ecommerce", "online shopping"],
  robots: "index, follow",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        suppressHydrationWarning
      >
        {children}
      </body>
    </html>
  );
}
